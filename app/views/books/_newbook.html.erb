<h2>User Info</h2>

	<%= attachment_image_tag user, :image, :fill, 100,100,format: 'jpg', fallback: "noimage.png",:size => "60×60" %>
	<p>name<%= user.name %></p>
	<p>introduction<%= user.introduction %></p>

	<%= link_to "", edit_user_path(current_user),class:"glyphicon glyphicon-wrench" %>

	<h2>New book</h2>

	<%= form_for(book) do |f| %>

	    <h4>Title</h4>
	    <%= f.text_field :title %>

	    <h4>Opinion</h4>
	    <%= f.text_area :body %>

	    <%= f.submit 'Create Book',class: "btn btn-primary" %>

	<% end %>